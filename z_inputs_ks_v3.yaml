opnstack_client_config:
  user_domain_name: { get_secret: username }
  password: { get_secret: password }
  auth_url: { get_secret: auth_url }
  region_name: { get_secret: region }
  project_domain_name: { get_secret: project_name }
